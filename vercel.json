{
  "buildCommand": "npm run build -w packages/shared && npm run build -w packages/cards && npm run build -w packages/games/spades && npm run build -w packages/client",
  "outputDirectory": "packages/client/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
